using System;
using System.Collections.Generic;
//using System.Linq;
using System.Text;

namespace RegistryLib
{
    internal class Program
    {
        /*
        static void Main(string[] args)
        {
            
            string regPath = @"Software\FuckUp\Params";
            string regName = @"AssHole";
            string regVal = @"33";
            
            if (RegistryProcessor.setToRegistry(regPath, regName, regVal, RegistryProcessor.RegistryParts.HKEY_CURRENT_USER) != State.ErrorCode.ALL_RIGTH)
                Console.WriteLine("Error...");
            else
                Console.WriteLine("Done...");
            Console.ReadLine();
            
            if (RegistryProcessor.getFromRegistry(regPath, regName, ref regVal, RegistryProcessor.RegistryParts.HKEY_CURRENT_USER) != State.ErrorCode.ALL_RIGTH)
                Console.WriteLine("Error...");
            else
                Console.WriteLine(regVal);
            Console.ReadLine();

        }
        */
    }
}
